PKG_NAME="{{ name }}"
PKG_DIR="{{ package_dir }}/${PKG_NAME}"

BASE_DIR="/opt"
HOME_DIR="${BASE_DIR}/${PKG_NAME}"

echo "installing ${PKG_NAME}..."
sudo mkdir -p ${HOME_DIR}
cd ${HOME_DIR}
sudo npm install ${PKG_DIR}/cosmo-ui*